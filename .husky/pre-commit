echo "🚨 Zero Trust Policy enabled 🗿. Checking for build errors before committing 🛠️"
pnpm build | grep -q "error" && echo "❌ Build failed. Please fix the errors before committing 🙂" && exit 1
echo "✅ Build successful. Committing changes 🚀"